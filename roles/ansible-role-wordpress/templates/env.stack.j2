#
# {{ item.service |upper }}
#
{% if item.extra_parms is defined and item.extra_parms |length > 0         -%}
{%   for k in item.extra_parms                                             -%}
{{      k + '=' + item.extra_parms[k]|string                                }}
{%   endfor                                                                -%}
{% endif                                                                   -%}